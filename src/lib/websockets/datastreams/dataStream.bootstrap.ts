// import { FulfillmentStatsStreamAdapter } from "@/routes/shop/fulfillment/streams/fulfillment-stats.adapter";

// const adapters = [
//     new FulfillmentStatsStreamAdapter(),
// ];

// adapters.forEach((adapter) => {
//     Promise.resolve(adapter.initialize()).catch((error) => {
//         console.error(`[DataStreams] Failed to initialize adapter`, error);
//     });
// });

export const initializedDataStreamAdapters = undefined;
